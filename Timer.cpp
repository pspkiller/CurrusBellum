/*
 * Timer.cpp
 *
 *  Created on: 15 nov. 2010
 *      Author: PsPKiller
 */

#include "Timer.h"

Timer::Timer()
{

}

void Timer::addTimer(int idTimer, f32 Timing)
{
	std::pair<int, f32> t(idTimer, Timing);
	std::pair<f32, f32> t2(0, 0);
	//timers.insert();
}

bool Timer::checkTimer(int idTimer)
{

	return false;
}
